/**
 * Placeholder. Will be replaced with an AI translation API call that
 * returns the Chinese translation for each English sentence.
 */
export async function translateSentences(
  sentences: string[],
): Promise<string[]> {
  return sentences.map(() => "");
}
